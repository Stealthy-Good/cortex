# Cortex Dashboard Environment Variables (server-side only — never exposed to browser)
# Copy this to .env.local and fill in your values

CORTEX_API_URL=http://localhost:3000
CORTEX_API_KEY=your-cortex-api-key
CORTEX_TENANT_ID=your-tenant-id
